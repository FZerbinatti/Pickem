<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.francesco.pickem">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.InfoActivity"></activity>
        <activity android:name=".Activities.EloActivity" />
        <activity android:name=".Activities.NewTrackEloDay" />
        <activity android:name=".Activities.EloTrackerActivity" />
        <activity android:name=".Activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.NotificationTeamActivity" />
        <activity android:name=".Activities.NotificationRegionActivity" />
        <activity android:name=".Activities.ResetPasswordActivity" />
        <activity android:name=".Activities.StatsActivity" />
        <activity android:name=".Activities.SettingsActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.LoginActivity">

        </activity>
        <activity android:name=".Activities.CalendarActivity" />
        <activity android:name=".Activities.PicksActivity" />
    </application>

</manifest>